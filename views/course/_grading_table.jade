tbody
  each grade in grades
    tr
      td= grade.student_number
      if gradeType === 0
        if grade.value === 'W'
          td
            h1= i18n('withdrewLabel')
        else
          td
            if grade.value === 'A'
              input(type='radio', name= grade.grade_id, value='A' checked)
            else
              input(type='radio', name= grade.grade_id, value='A')
          td
            if grade.value === 'B'
              input(type='radio', name= grade.grade_id, value='B' checked)
            else
              input(type='radio', name= grade.grade_id, value='B')
          td
            if grade.value === 'C'
              input(type='radio', name= grade.grade_id, value='C' checked)
            else
              input(type='radio', name= grade.grade_id, value='C')
          td
            if grade.value === 'D'
              input(type='radio', name= grade.grade_id, value='D' checked)
            else
              input(type='radio', name= grade.grade_id, value='D')
          td
            if grade.value === 'F'
              input(type='radio', name= grade.grade_id, value='F' checked)
            else
              input(type='radio', name= grade.grade_id, value='F')
          td
            if grade.value === 'X'
              input(type='radio', name= grade.grade_id, value='X' checked)
            else
              input(type='radio', name= grade.grade_id, value='X')
          td
            if grade.incomplete
              input(type='checkbox', name= grade.grade_id + ':inc' checked)
            else
              input(type='checkbox', name= grade.grade_id + ':inc')
      else if gradeType === 1
        if grade.value === 'W'
          td
            h1= i18n('withdrewLabel')
        else
          td
            if grade.value === 'PS'
              input(type='radio', name= grade.grade_id, value='PS' checked)
            else
              input(type='radio', name= grade.grade_id, value='PS')
          td
            if grade.value === 'PN'
              input(type='radio', name= grade.grade_id, value='PN' checked)
            else
              input(type='radio', name= grade.grade_id, value='PN')
          td
            if grade.value === 'PB'
              input(type='radio', name= grade.grade_id, value='PB' checked)
            else
              input(type='radio', name= grade.grade_id, value='PB')
          td
            if grade.value === 'P'
              input(type='radio', name= grade.grade_id, value='P' checked)
            else
              input(type='radio', name= grade.grade_id, value='P')
          td
            if grade.value === 'NP'
              input(type='radio', name= grade.grade_id, value='NP' checked)
            else
              input(type='radio', name= grade.grade_id, value='NP')
          td
            if grade.value === 'X'
              input(type='radio', name= grade.grade_id, value='X' checked)
            else
              input(type='radio', name= grade.grade_id, value='X')
          td
            if grade.incomplete
              input(type='checkbox', name= grade.grade_id + ':inc' checked)
            else
              input(type='checkbox', name= grade.grade_id + ':inc')
      else if gradeType === 2
        if grade.value === 'W'
          td
            h1= i18n('withdrewLabel')
        else
          td
            if grade.value === 'P'
              input(type='radio', name= grade.grade_id, value='P' checked)
            else
              input(type='radio', name= grade.grade_id, value='P')
          td
            if grade.value === 'NP'
              input(type='radio', name= grade.grade_id, value='NP' checked)
            else
              input(type='radio', name= grade.grade_id, value='NP')
          td
            if grade.value === 'X'
              input(type='radio', name= grade.grade_id, value='X' checked)
            else
              input(type='radio', name= grade.grade_id, value='X')
          td
            if grade.incomplete
              input(type='checkbox', name= grade.grade_id + ':inc' checked)
            else
              input(type='checkbox', name= grade.grade_id + ':inc')